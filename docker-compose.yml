services:
  shopware:
    # Built from the local Dockerfile: the stock dockware image plus shopware-cli,
    # so the plugin can be validated from inside its own test environment.
    build:
      context: .
      dockerfile: Dockerfile
    image: 'kommandhub-flutterwave-plugin:6.7.8.0'
    container_name: shopware-flutterwave
    environment:
      - 'XDEBUG_ENABLED=0'
      - 'PHP_VERSION=8.3'
    volumes:
      # ----------------------------------------------------------------------------------------------------------------------------------
      # PLUGINS
      - "./:/var/www/html/custom/static-plugins/KommandhubFlutterwaveSW"
      - "database:/var/lib/mysql"
      - "./Makefile:/var/www/html/Makefile"
      # ----------------------------------------------------------------------------------------------------------------------------------
      # EXCLUDE
      - "/var/www/html/custom/static-plugins/KommandhubFlutterwaveSW/.git/"
      - "/var/www/html/custom/static-plugins/KommandhubFlutterwaveSW/node_modules/"
      - "/var/www/html/custom/static-plugins/KommandhubFlutterwaveSW/vendor/"
      # ----------------------------------------------------------------------------------------------------------------------------------

volumes:
  database:
    driver: local